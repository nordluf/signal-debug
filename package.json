{
  "name": "signal-debug",
  "version": "0.0.0-development",
  "description": "Start customisable debug output on USR2 signal.",
  "main": "index.js",
  "scripts": {
    "test": "DEBUG_COLORS=no mocha",
    "test:coverage": "DEBUG_COLORS=no istanbul cover _mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | run.env codacy-coverage && rm -rf ./coverage",
    "lint": "semistandard",
    "semantic-release": "semantic-release",
    "travis-deploy-once": "travis-deploy-once"
  },
  "husky": {
    "hooks": {
      "commit-msg": "validate-commit-msg",
      "pre-commit": "npm test && npm run lint"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nordluf/signal-debug.git"
  },
  "keywords": [
    "debug",
    "logging"
  ],
  "author": "Evgeny Kruglov",
  "license": "JSON",
  "bugs": {
    "url": "https://github.com/nordluf/signal-debug/issues"
  },
  "engines": {
    "node": ">= 4.8.0",
    "npm": ">= 2.14.0"
  },
  "homepage": "https://github.com/nordluf/signal-debug#readme",
  "devDependencies": {
    "codacy-coverage": "^3.4.0",
    "husky": "^1.3.1",
    "istanbul": "^0.4.5",
    "mocha": "^6.1.4",
    "mocha-lcov-reporter": "^1.3.0",
    "run.env": "^1.1.0",
    "semantic-release": "^15.13.12",
    "semistandard": "^11.0.0",
    "travis-deploy-once": "^5.0.11",
    "validate-commit-msg": "^2.14.0"
  },
  "semistandard": {
    "globals": [
      "describe",
      "before",
      "beforeEach",
      "after",
      "it"
    ]
  },
  "dependencies": {
    "debug": "^3.2.6"
  }
}
